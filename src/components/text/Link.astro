---
import { Icon } from 'astro-icon/components'

export interface Props {
  text: string
  url: string
  arrowLeft?: boolean
  newWindow?: boolean
  linkClass?: string
  iconClass?: string
}

const { text, url, arrowLeft, newWindow, linkClass, iconClass } = Astro.props
---

<a
  href={url}
  target={newWindow ? '_blank' : '_self'}
  rel={newWindow ? 'noopener noreferrer' : ''}
  class={`${linkClass} flex gap-2 items-center text-purple hover:text-grey group`}
  aria-label={text}
>
  {
    arrowLeft ? (
      <>
        <Icon
          name="tabler:arrow-narrow-left"
          class={`${iconClass} group-hover:-translate-x-1 transition-transform duration-(--transition) ease-in-out`}
        />
        {text}
      </>
    ) : (
      <>
        {text}
        <Icon
          name="tabler:arrow-narrow-right"
          class={`${iconClass} group-hover:translate-x-1 transition-transform duration-(--transition) ease-in-out`}
        />
      </>
    )
  }
</a>
