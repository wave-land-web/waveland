---
export interface Props {
  class?: string | object
}

const { class: className } = Astro.props
---

<article class:list={[`${className} relative w-full rounded-2xl shadow-lg`, className]}>
  <slot />
</article>
